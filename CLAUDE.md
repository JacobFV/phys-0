# Agent Handoff For phys-0

The shared agent handoff lives in:

```text
docs/agent-handoff.md
```

Read that file before operating hardware. Keep durable project, hardware,
calibration, operation, and Chemist-0 experiment context in docs instead of
duplicating it across `AGENTS.md`, `GEMINI.md`, and `CLAUDE.md`.
